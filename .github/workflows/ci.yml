name: CI
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - uses: actions/setup-node@v3
    - run: ./bin/setup
    - run: ./bin/test
    - run: ./bin/lint
    - run: ./bin/build
