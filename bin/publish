#!/bin/bash

# TODO: check if version bumped - perhaps by checking sentry-cli releases

./bin/build                 && \
./bin/upload_sourcemap      && \
echo "TODO: publish to NPM" && \
echo "TODO: what else?"
